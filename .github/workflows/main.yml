---
name: Yaml Lint
on: [push]
jobs:
  lintAllTheThings:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@main
    - name: yaml-lint
      uses: ibiqlik/action-yamllint@master
      with:
        file_or_dir: windows/*.yml linux/*.yml traefik_v1/*.yml
